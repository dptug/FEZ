// Type: SharpDX.Win32.VariantElementType
// Assembly: SharpDX, Version=2.4.2.0, Culture=neutral, PublicKeyToken=627a3d6d1956f55a
// MVID: 578390A1-1524-4146-8C27-2E9750400D7A
// Assembly location: F:\Program Files (x86)\FEZ\SharpDX.dll

namespace SharpDX.Win32
{
  public enum VariantElementType : ushort
  {
    Empty = (ushort) 0,
    Null = (ushort) 1,
    Short = (ushort) 2,
    Int = (ushort) 3,
    Float = (ushort) 4,
    Double = (ushort) 5,
    Currency = (ushort) 6,
    Date = (ushort) 7,
    BinaryString = (ushort) 8,
    Dispatch = (ushort) 9,
    Error = (ushort) 10,
    Bool = (ushort) 11,
    Variant = (ushort) 12,
    ComUnknown = (ushort) 13,
    Decimal = (ushort) 14,
    Byte = (ushort) 16,
    UByte = (ushort) 17,
    UShort = (ushort) 18,
    UInt = (ushort) 19,
    Long = (ushort) 20,
    ULong = (ushort) 21,
    Int1 = (ushort) 22,
    UInt1 = (ushort) 23,
    Void = (ushort) 24,
    Result = (ushort) 25,
    Pointer = (ushort) 26,
    SafeArray = (ushort) 27,
    ConstantArray = (ushort) 28,
    UserDefined = (ushort) 29,
    StringPointer = (ushort) 30,
    WStringPointer = (ushort) 31,
    Recor = (ushort) 36,
    IntPointer = (ushort) 37,
    UIntPointer = (ushort) 38,
    FileTime = (ushort) 64,
    Blob = (ushort) 65,
    Stream = (ushort) 66,
    Storage = (ushort) 67,
    StreamedObject = (ushort) 68,
    StoredObject = (ushort) 69,
    BlobObject = (ushort) 70,
    ClipData = (ushort) 71,
    Clsid = (ushort) 72,
    VersionedStream = (ushort) 73,
    BinaryStringBlob = (ushort) 4095,
  }
}
